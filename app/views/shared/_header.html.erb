<nav class="navbar navbar-expand-lg bg-dark navbar-dark">
  <%= link_to 'Logo', root_path, class: 'navbar-brand' %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item">
        <%= link_to t('.mailer'), '/rails/mailers/tests_mailer/', class: 'nav-link' %>
      </li>

      <% if current_user && current_user.admin? %>
        <%= link_to t('.admin'), admin_root_path, class: 'nav-link' %>
        <%= link_to t('.gist_list'), admin_gists_path, class: 'nav-link' %>
      <% end %>

      <li class="nav-item">
        <span class="navbar-text text-info">
          <%= l(Time.current, format: :short) %>
        </span>
      </li>
    </ul>

    <ul class="navbar-nav my-2 my-lg-0">
      <%= render 'shared/auth' %>
    </ul>
  </div>
</nav>
