<header>
	<div class="header">
		<% if user_signed_in? %>
			<p>Welcome, <%= current_user.username %>!</p>

			<%= button_to "logout", destroy_user_session_path, method: :delete  %>
		<% end %>
	</div>
</header>
